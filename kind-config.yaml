kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

# Single control-plane node - this project demonstrates the pattern
# (single-broker Kafka, single-node Elasticsearch) rather than a
# highly-available deployment, so no worker nodes are needed either.
nodes:
  - role: control-plane
